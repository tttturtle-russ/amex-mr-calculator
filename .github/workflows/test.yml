name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  data-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Data self-check
        run: node test.js
